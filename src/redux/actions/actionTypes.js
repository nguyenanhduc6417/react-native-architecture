const loading = {
  setStatus: 'LOADING_SET_STATUS',
  setMessage: 'LOADING_SET_MESSAGE'
}

const login = {
  setLogin: 'LOGIN_SET_LOGIN',
  setSenha: 'LOGIN_SET_SENHA'
}

const usuario = {
  setUsuario: 'USUARIO_SET_OBJ'
}

const actionTypes = {
  loading,
  login,
  usuario
}

export default actionTypes
