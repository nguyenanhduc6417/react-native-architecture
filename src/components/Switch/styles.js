import sizes from '../../values/sizes'
import colors from '../../values/colors'

const styles = {
  container: {
    marginTop: 6
  },
  containerSwith: {
    marginTop: 16
  },
  label: {
    marginTop: 16,
    marginLeft: 10,
    color: colors.primary,
    fontSize: sizes.font.input - 3
  }
}

export default styles
